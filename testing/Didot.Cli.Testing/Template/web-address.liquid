# {{ model.Hostname }}

## Address

- Scheme: {{ model.Scheme }}
- Host: {{ model.Hostname }}
- Port: {%- if model.Port -%}
{{- }} {{model.Port -}}
{%- else -%}
{{- }} No port found.{{ -}}
{%- endif %}

## Paths

{%- if model.Paths.size > 0 -%}
| Index | Path                |
|-------|---------------------|
{%- for path in model.Paths -%}
| {{ forloop.index }} | /{{ path }} |
{%- endfor -%}
{% else -%}
No paths found.
{%- endif -%}

## Query Strings

{%- if model.QueryStrings.size > 0 -%}
| Parameter      | Value         |
|----------------|---------------|
{%- for query in model.QueryStrings -%}
| {{ query.Key }} | {{ query.Value }} |
{%- endfor -%}
{% else -%}
No query strings found.
{%- endif -%}

## Section

{%- if model.Section -%}
{{ model.Section }}
{%- else -%}
No section found.
{%- endif -%}
